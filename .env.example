# Copy to .env and fill in real values (do not commit .env)

# Outlook / Office 365 SMTP
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=concierge@sdcorp.in
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=concierge@sdcorp.in

# Daily feedback report recipients (comma-separated)
FEEDBACK_REPORT_TO=operations.manager@theimperialclub.in,dutymanager.icbtl@theimperialclub.in
FEEDBACK_REPORT_CC=kshitij.gupta@sdcorp.in
